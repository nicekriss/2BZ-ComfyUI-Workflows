# 최종 선택과 실측 · 2026-09-07

**최종은 FastH3 + USDU 워크플로우입니다.** 후속 단일 노드 방식도 완주했지만 사용자가 더 망가지는 부분을 확인했고, 약1분의 속도 차이보다 결과 보존을 선택했습니다. 모든 영상에서의 우열을 보장하는 판정은 아닙니다.

## 동일 15초 비교 (2026-09-06)

| 방식 | 성공 실행 시간 | 일반 H3+Turbo 대비 |
|---|---:|---:|
| 일반 H3 + 4-step Turbo LoRA / USDU / 2 steps | 5797.284초 · 96분37초 | 1.00× |
| FastH3 / USDU / 2 steps / VSA10% | 2920.586초 · 48분40초 | 1.985× |
| FastH3 / MMH3 단일 노드 / 2 steps / VSA10% | 2856.478초 · 47분36초 | 2.029× |

동일 영상 앞15초,24fps. 360프레임에 마지막2프레임을 패딩해362프레임 처리 후 같은2장을 제거했습니다.
원본960×544 → 약1MP 정규화1376×768 → 2배2752×1536. Euler/simple/2 steps/denoise0.20/seed1, 같은 프롬프트·비디오VAE·인코더·로컬RealESRGAN_x2.pth. RTX3090 24GB / RAM64GB.
일반 모델은 minimax_h3_ref2va_pruned_int8_convrot + minimax_h3_turbo_v4_step600_ema_pruned_comfyui(strength1), Fast는 minimax_h3_fastvideo_vsa_datafree_1300step_4step_int8_convrot입니다.

A/B USDU 타일은1376×768,Chess,padding64,maskblur16,seamNone,Reprocess Overlap,batch_size1,anchor_context false,tiled_decode false.
C는 픽셀ESRGAN → VAE → MMH3 latent 타일입니다. 초기 실패한 bilinear latent 확대 경로와 다릅니다. 공간2행4열(실제832×832,겹침H128/W192),시간85/17,anchor.999.

측정은 ComfyUI execution_start~success, 큐 대기 제외, 실행 중 로딩·저장 및 캐시 효과 포함입니다. 각1회 성공이며 동일한 warm/cold 반복 측정이 아닙니다.
A/B/C의 cached_nodes 개수는 각각11/9/14였으며 구현과 메모리 정책이 다릅니다. 같은 seed가 같은 픽셀 난수 배치를 뜻하지도 않습니다.
따라서 **“이번 같은 작업 조건에서 FastH3 구성이 약2배 빨랐다”**까지 말할 수 있습니다. VSA만의 효과를 분리한 숫자가 아닙니다.
B와C는64.108초 차이(약2.2%)라 확실한 속도 우위로 일반화하지 않습니다.

## OOM과 메모리 보완

B의 앞선 두 full 실행은2143.691초와2542.020초 후 OOM, 완성 결과 없음. 실패 시간 합78분6초를 위 성공 시간에 숨겨 넣지 않았습니다.
성공 실행까지 합친 B의 full-attempt 시간은126분46초(대기/분석/smoke 제외)입니다.
성공 B는 KJNodes ModelMemoryUsageFactorOverride로 메모리 추정 계수0.114→1.0을 적용했습니다. 모델/steps/denoise/해상도는 그대로, 샘플링 준비의 메모리 추정·오프로딩 정책이 다릅니다.
기본 배포에는 이 선택 노드를 넣지 않았습니다. [MEMORY-ko.md](MEMORY-ko.md).
길이가 유일한 원인이라고 확정하지 않았고, 사용자의 이전30초 성공 사례도 있습니다. 설정 변경이 유일한 성공 원인이었다는 통제 검증도 없습니다.

## 배포와 측정의 차이

- 배포는 공식 RealESRGAN_x2plus.pth, 위 실측은 기존 로컬RealESRGAN_x2.pth. 파일 동일성 미검증이므로 완전히 같은 아티팩트 벤치마크라고 주장하지 않습니다.
- 배포 기본은 추가 메모리 노드 없는 USDU이며, 전체 영상/원본fps 연결을 유지합니다. 위 비교는 고정24fps/15초/패딩362프레임.
- 배포 기본을2배/.20으로 갱신했지만 새 ZIP으로 깨끗한 PC의 설치부터 렌더까지 재검증한 것은 아닙니다.
- 메인4070 Ti SUPER16GB/RAM32GB 기존 업스케일 성공은 사용자 확인입니다. 이번3090 타이밍표·모든 길이·동시 녹화를 보장하지 않습니다.

## 이전 기록과 구분

| 이전 실험 | 관측 | 한계 |
|---|---|---|
| 사용자15초·1.8배 | 29분40초 | 이번2배 비교와 다름 |
| VSA OFF/ON | 2회 평균188.61초/187.22초 | 다른 소스22프레임·16fps·2배·.25·warm,회차별 승자 반전; 유의미한 전체 가속 미입증 |
| 초기 bilinear latent 대안 | 22프레임103.735초 | 흰 무늬·인물 변형으로 실패; 공식 픽셀SR 배선과 다름 |

“latent15초 비교 미실행”은 더 이상 최신 결론이 아닙니다. 후속 픽셀SR 기반 단일 노드는15초 완주했고, 사용자 화질 검토 후 미채택했습니다.
샘플러만의 시간과 전체 시간을 구분하세요. C의 샘플러는696.2635초지만 전체는2856.478초이며, VAE encode683.124초/decode494.5531초/픽셀SR473.126초 등이 있습니다.

## 실행 식별자

- A:93f3ca0a-5ee0-4e4c-ab38-46a449c77183
- B 성공:c4090d94-3027-4369-b270-a513b021548c
- C:f7e079ae-1715-46f9-95e1-b1f2e86d5782
- B 실패:a55144d5-37e9-4966-8e81-754a9b307825,c3bf7720-de4e-4e2b-b159-e9e4e2e91b15

기록은 제작자 보관 history 및 실제 출력에 근거합니다. 개인 원본 영상·로컬 경로·전체 실행 프롬프트는 이 패키지에 포함하지 않습니다.
